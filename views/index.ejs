<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8"/>
		<link rel="stylesheet" href="bulma.min.css">
		<script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"> </script>
	</head>
	<body>
		<section class="section">
			<div class="maincolumns columns is-desktop is-vcentered">
				<div class="column is-10 is-offset-1 notification is-info is-light box container">
					<div class="title">
						Person
					</div>
					<% if (locals.allPeople) { %>
						<div class="table-container">
						<table class="table is-bordered is-hoverable is-striped is-narrow">
							<thead>
							<tr>
								<th> First Name </th>
								<th> Last Name </th>
								<th> Skill List</th>
								<th> &nbsp; </th>
							</tr>
							</thead>
							<% let i=0; %>
							<% for(i=0; i < allPeople.length; i++) { %>
							<tr>
								<td><%= allPeople[i].first_name %></td>
								<td><%= allPeople[i].last_name %></td>
								<td><%= allPeople[i].skill_list %></td>
								<td><a href="/deletePerson?id=<%= allPeople[i].person_id %>">
									<span class="icon is-small">
										<i class="fas fa-user-times"></i>
									</span></a>
								</td>
							</tr>
							<% } %>
							<tfoot>
								<tr> <td colspan="4">
								There are <%= i %> people.
								</td> </tr>
							</tfoot>
						</table>
						</div>
					<% } %>

					<form action="addPerson" method="post">
						<div class="field is-horizontal">
						  <div class="field-label is-normal">
							<label class="label">Add New Person</label>
						  </div>
						  <div class="field-body">
							<div class="field">
							  <p class="control is-expanded">
								<input name="first_name" class="input" type="text" placeholder="First Name">
							  </p>
							</div>
							<div class="field">
							  <p class="control is-expanded">
								<input name="last_name" class="input" type="text" placeholder="Last Name">
							  </p>
							</div>							
							<div class="field">
								<button class="button is-link">
									<span>Add</span>
									<span class="icon is-small">
										<i class="fas fa-user-plus"></i>
									</span>
								</button>
							</div>
						  </div>
						</div>
					</form>
				</div>
			</div>
		</section>
	</body>
</html>
